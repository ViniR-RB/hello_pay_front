import 'package:flutter/material.dart';

class TransactionAdminHome extends StatelessWidget {
  const TransactionAdminHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Admin'),
      ),
      body: Container(),
    );
  }
}
